export const apiUrl = 'http://localhost:3200';

export const appUrl = 'http://localhost:3000';

export function capitalizeFirstLetter(string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
  }